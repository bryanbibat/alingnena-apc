<tr>
  <td><%=h item.name %></td>
  <td><%=h item.description %></td>
  <td><%=number_to_currency item.cost, :unit => 'PhP' %></td>
  <td><%=h item.stock %></td>
  <td><%= link_to 'Show', product_url(item.id) %></td>
  <td><%= link_to 'Edit', edit_product_url(item) %></td>
</tr>
